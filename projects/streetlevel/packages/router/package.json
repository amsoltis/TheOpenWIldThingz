{
  "name": "@streetlevel/router",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Multi-criteria shortest-path routing over the real MTA station graph, with transfer penalties and service-alert diversion.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@streetlevel/shared": "*",
    "@streetlevel/data": "*"
  }
}
