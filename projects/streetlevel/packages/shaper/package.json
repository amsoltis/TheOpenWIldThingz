{
  "name": "@streetlevel/shaper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Turns a routed path into plain-English instruction cards. Deterministic compiler by default; an Nvidia NIM pass upgrades the prose when a model endpoint is configured.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@streetlevel/shared": "*",
    "@streetlevel/data": "*",
    "@streetlevel/router": "*"
  }
}
